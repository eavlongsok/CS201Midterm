To compile the main program, type:

g++ main.cpp database.cpp -o main.exe

then to run the program,
.\main

==========================================
or just type:
./build.bat     (in VSCode)
build.bat       (in CMD)
